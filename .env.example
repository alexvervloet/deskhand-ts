# Copy to .env and edit. Every value below has a working default, so the app
# runs with no .env at all — keyless, against the scripted mock model.

# --- Database -------------------------------------------------------------
DATABASE_URL=postgresql://deskhand:deskhand@localhost:5438/deskhand

# --- Model ----------------------------------------------------------------
# Leave ANTHROPIC_API_KEY unset to run against the scripted mock provider.
# The mock is not a silent stub: every run it drives is tagged provider=mock
# in the API response and in the run viewer, so a demo can never be mistaken
# for a real model call.
# ANTHROPIC_API_KEY=sk-ant-...
MODEL_ID=claude-sonnet-5
MODEL_EFFORT=high

# --- Run bounds -----------------------------------------------------------
# Per-run ceilings. A run that hits any of these lands in the `exhausted`
# terminal state with the specific bound named, never a silent stall.
MAX_STEPS_PER_RUN=24
MAX_TOKENS_PER_RUN=400000
MAX_WALLCLOCK_SECONDS_PER_RUN=900
MAX_SPEND_USD_PER_RUN=2.00

# Repeated identical tool calls before the runtime calls it a loop.
LOOP_DETECTION_THRESHOLD=3

# --- Spend ceilings -------------------------------------------------------
# Per-org daily cap, and the one that actually bounds the deployment's bill.
DAILY_BUDGET_USD_PER_ORG=10.00
PLATFORM_DAILY_BUDGET_USD=50.00

# --- Payout ceilings ------------------------------------------------------
# Everything above bounds what the agent costs to run. These bound what it
# hands back, which is the merchant's money and the reason the project exists.
# In cents, because money here is integer cents everywhere.
#
# Enforced inside issue_refund, at the point of payment, so they hold even
# after a human has clicked approve — consent is for one payment, not a waiver
# of the limit. The per-run figure is snapshotted onto the run at creation, so
# raising it here does not widen a run that is already in flight.
MAX_REFUND_CENTS_PER_RUN=100000
DAILY_REFUND_CENTS_PER_ORG=500000

# --- Approvals ------------------------------------------------------------
# An irreversible tool call suspends the run until a human decides. If nobody
# does within this window the run fails with `approval_expired` — a distinct
# terminal reason, never a silent denial.
APPROVAL_TTL_SECONDS=86400

# --- Server ---------------------------------------------------------------
PORT=8000
HOST=0.0.0.0
# Comma-separated origins allowed to call this API from a browser. The Vite dev
# server is the only one that needs it locally.
CORS_ORIGINS=http://localhost:5173

# --- Deployment -----------------------------------------------------------
# Behind a proxy the socket peer is the proxy, so the login throttle would see
# every visitor as one caller and the first bad password would lock out
# everyone. Name the header your proxy sets *and overwrites* — Fly-Client-IP,
# X-Real-IP — never one a client can append to, such as X-Forwarded-For.
# CLIENT_IP_HEADER=Fly-Client-IP

# Run the agent inside the API process instead of as its own service. Wrong for
# production, where the two scale and fail independently; right for a demo
# machine that should be allowed to sleep when nobody is looking at it.
# RUN_WORKER_INLINE=1

# --- Observability --------------------------------------------------------
# Nothing to configure. Every model call and tool call is a row in `steps`
# carrying tokens, cost, latency, arguments and result, and the runtime emits a
# structured JSON line per event on the `deskhand.events` logger for whatever
# collects your logs.
